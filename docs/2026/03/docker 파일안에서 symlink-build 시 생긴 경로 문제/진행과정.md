# 진행과정 — 2026-03-25 prefix chain의 setup.bash 기록 오류 및 docker container와 호스트 간 심볼릭 링크 오류

**관련 TIL**: `TIL_2026_02_24_RF2O_SLAM_Integration.md`  
**이전 작업**: `없음`

---

## 0. 시작 상태
<img src="/home/user/my-til/docs/2026/03/images/260325_not_found_isaac/image.png" width="100%" height="100%"/>

터미널이 새로 시작될 때마다 isaac_ws_ros 관련 setup.bash를 찾지 못하는 상태:
- 첫번째 의심점 : 누가 해당 setup.bash들을 source 하려고 시도하는가?
- 두번째 의심점 : docker container 내부에서 빌드된 setup.bash들을 source 하려고 시도하기 때문에 생기는 문제인가?
---

## 1. Shell startup config 점검

### 1.1.1 source 위치 확인

grep 명령어로 ROS2 워크스페이스의 빌드 결과물(setup.bash) 내에서도 해당 경로가 어떻게 참조되고 있는지 확인:
```bash
grep -n "isaac_ws_ros" ~/.bashrc
# ~/.bashrc 파일 안에서 "isaac_ws_ros"라는 문자열이 포함된 줄을 찾으라는 명령
# -n 옵션은 그 내용이 파일의 몇 번째 줄에 있는지 표시
```

### 1.1.2 확인 결과

```bash
162:export ISAAC_ROS_WS=~/isaac_ws_ros
# 162:: .bashrc 파일의 162번째 줄에 해당 내용이 있다는 뜻
# export ISAAC_ROS_WS=...: ISAAC_ROS_WS라는 이름의 환경 변수를 선언하고 외부(터미널 세션 및 실행 프로그램)에서 사용할 수 있도록 내보낸다는 의미
# ~/isaac_ws_ros: 이 변수에 할당된 값
```

- 결론 : bashrc 에는 setup.bash를 직접 source 하는 명령이 없고, 단지 ISAAC_ROS_WS라는 환경변수만 선언되어 있음. 따라서 bashrc에서 setup.bash를 source 하려고 시도하는 것은 아님.

---
### 1.2.1 source 위치 확인2

grep 명령어로 쉘 설정 파일 (`~/.bashrc`)에서 `isaac_ws_ros` 문자열이 포함된 줄을 검색하여 어떤 명령이 실행되고 있는지 확인:
```bash
grep -rn "isaac_ws_ros" ~/.bashrc ~/.bash_profile ~/.profile 2>/dev/null
# 홈 디렉토리의 주요 쉘 설정 파일들에서 "isaac_ws_ros"라는 문자열이 포함된 줄을 찾으라는 명령
# -r 옵션은 하위 디렉토리까지 재귀적으로 검색
# -n 옵션은 그 내용이 파일의 몇 번째 줄에 있는지 표시
# 2>/dev/null은 검색 중 발생할 수 있는 오류 메시지(2, stderr)를 무시하고 출력하지 않도록 하는 부분

grep -rn "isaac_ws_ros" /home/jetson/jupiter_ws_ros2/install/setup.bash 2>/dev/null | head -5
# setup.bash 파일 내에서 "isaac_ws_ros"라는 문자열이 포함된 줄을 찾으라는 명령
# head -5는 검색 결과 중 상위 5줄만 출력하라는 의미
```


### 1.2.2 확인 결과

```bash
/home/jetson/.bashrc:162:export ISAAC_ROS_WS=~/isaac_ws_ros
# ~/.bashrc 파일의 162번째 줄에 해당 내용이 있다는 뜻
# export ISAAC_ROS_WS=...: ISAAC_ROS_WS라는 이름의 환경
31:COLCON_CURRENT_PREFIX="/home/jetson/isaac_ws_ros/install"
# 이 부분이 중요. 이 출력은 jupiter_ws_ros2/install/setup.bash 파일(혹은 그와 연결된 파일) 내부에서 나온 것
# colcon으로 빌드된 ROS2 패키지들이 실제로 설치된 위치(install 폴더)를 가리킴
# 해석: 현재 jupiter_ws_ros2 환경을 로드(source)하면, 시스템은 isaac_ws_ros 워크스페이스에 설치된 패키지들도 함께 참조하게 됨.
#즉, 두 워크스페이스가 서로 Overlays(계층 구조)로 연결되어 있거나, jupiter_ws_ros2를 빌드할 때 isaac_ws_ros 환경 위에서 빌드되었다는 증거
```

- 구조적 이해
- 현재 시스템은 아래와 같은 계층 구조를 가지고 있을 가능성이 높음:
  - Underlay: /opt/ros/humble (기본 ROS2 설치 경로)
  - Middle-layer: ~/isaac_ws_ros/install (Isaac ROS 관련 패키지)
  - Overlay: ~/jupiter_ws_ros2/install (사용자의 추가 프로젝트 패키지)
- 이런 설정 덕분에 jupiter_ws_ros2에서 작업하면서도 isaac_ws_ros에 있는 기능(예: vSLAM, NvBlox 등)을 그대로 가져다 쓸 수 있는 것임.
---

## 2. Find source of isaac_ws_ros setup

### 2.1. setup.bash 파일의 앞 40줄을 출력하여 워크스페이스 적층(Chaining) 구조 확인
```bash
head -40 /home/jetson/jupiter_ws_ros2/install/setup.bash
```

### 2.2. 확인 결과

<details>
<summary>setup.bash 소스 코드 출력 (Click)</summary>

```bash
# generated from colcon_bash/shell/template/prefix_chain.bash.em

# 이 스크립트는 파일이 생성될 때 로드(source)되어 있던 다른 경로의 환경들을
# 현재 워크스페이스 환경과 통합(extend)하는 역할을 합니다.

# function to source another script with conditional trace output
# first argument: the path of the script
_colcon_prefix_chain_bash_source_script() {
  if [ -f "$1" ]; then
    if [ -n "$COLCON_TRACE" ]; then
      echo "# . \"$1\""
    fi
    . "$1"
  else
    echo "not found: \"$1\"" 1>&2
  fi
}

# [환경 체인] 하위 레이어부터 순차적으로 로드됨
# source chained prefixes
# setting COLCON_CURRENT_PREFIX avoids determining the prefix in the sourced script
COLCON_CURRENT_PREFIX="/opt/ros/humble"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"
# setting COLCON_CURRENT_PREFIX avoids determining the prefix in the sourced script
COLCON_CURRENT_PREFIX="/home/jetson/yahboomcar_ros2_ws/software/library_ws/install"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"
# setting COLCON_CURRENT_PREFIX avoids determining the prefix in the sourced script
COLCON_CURRENT_PREFIX="/home/jetson/yahboomcar_ros2_ws/yahboomcar_ws/install"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"
# setting COLCON_CURRENT_PREFIX avoids determining the prefix in the sourced script
COLCON_CURRENT_PREFIX="/home/jetson/isaac_ws_ros/install"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"

# [현재 워크스페이스] 최상단 레이어 로드
# source this prefix
COLCON_CURRENT_PREFIX="$(builtin cd "`dirname "${BASH_SOURCE[0]}"`" > /dev/null && pwd)"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"

unset COLCON_CURRENT_PREFIX
unset _colcon_prefix_chain_bash_source_script
```
</details>

### 2.3. 분석 결과 요약
- 현재 시스템은 5단계의 계층 구조로 얽혀 있음. jupiter_ws_ros2 환경만 로드(source) 하더라도 아래의 모든 기능을 사용할 수 있는 상태임

  - ROS2 Humble: 표준 시스템 라이브러리
  - library_ws: 야붐카 기초 드라이버 및 라이브러리
  - yahboomcar_ws: 야붐카 제어 및 센서 설정
  - isaac_ws_ros: NVIDIA Isaac ROS (vSLAM 등 고성능 비전 기능)
  - jupiter_ws_ros2: 현재 개발 중인 메인 워크스페이스
- 주의사항 : 중간 단계에 포함된 isaac_ws_ros 등의 폴더 위치를 변경하거나 삭제할 경우, 최상위 워크스페이스인 jupiter_ws_ros2의 환경 로드가 실패할 수 있음
---

### 2.4. 원인 분석

- 원인 : 
  -  jupiter_ws_ros2를 빌드(colcon build)할 때 setup.bash가 이미 source된 상태였기 때문에, colcon이 그 prefix chain을 setup.bash에 기록해 둠. 터미널을 열 때마다 .bashrc → setup.bash → local_setup.bash → 각 패키지의 local_setup.bash를 찾으려 하지만, Docker 안에서만 빌드된 패키지라 호스트에는 파일이 없어서 "not found" 메시지가 나옴.

---

## 3. 정확한 원인 더 분석

### 3.1.1 전체 워크스페이스 설치 확인
```bash
ls /home/jetson/isaac_ws_ros/install/local_setup.bash 2>&1
# 파일이 존재하는지 확인

```
### 3.1.2 출력결과

```bash
/home/jetson/isaac_ws_ros/install/local_setup.bash
```
- 결과 : 파일이 존재함
- 의미 : colcon build가 시도되었거나, 최소한 install 폴더 구조는 생성되었음. 즉, "이 폴더는 ROS2 워크스페이스가 맞다"는 최소한의 증거.

### 3.2.1 isaac_ws_ros 워크스페이스 내 패키지별 local_setup.bash 존재 여부 확인

```bash
ls /home/jetson/isaac_ws_ros/install/isaac_common/share/isaac_common/local_setup.bash 2>&1; ls /home/jetson/isaac_ws_ros/install/isaac_ros_visual_slam/share/isaac_ros_visual_slam/local_setup.bash 2>&1
# isaac_common과 isaac_ros_visual_slam 패키지의 local_setup.bash 파일이 존재하는지 확인
```

### 3.2.2 분석 결과
```bash
/home/jetson/isaac_ws_ros/install/isaac_common/share/isaac_common/local_setup.bash
/home/jetson/isaac_ws_ros/install/isaac_ros_visual_slam/share/isaac_ros_visual_slam/local_setup.bash
# 결과 : 두 패키지 모두 local_setup.bash 파일이 존재함
# 의미 : isaac_ws_ros 워크스페이스가 제대로 빌드되어 있고, 해당 패키지들이 설치되어 있다는 증거. 따라서 setup.bash가 존재하지 않는다는 것은 단순히 "파일이 없다"는 문제보다는, "파일이 존재하지만 접근할 수 없다"는 문제일 가능성이 높음. 즉, 심볼릭 링크 문제이나 권한 문제일 가능성이 높음.
```

# 3.3.1 루프를 통한 전수조사
```bash
for pkg in isaac_common isaac_ros_apriltag_interfaces isaac_ros_bi3d_interfaces isaac_ros_common isaac_ros_nova_interfaces isaac_ros_pointcloud_interfaces isaac_ros_r2b_galileo isaac_ros_tensor_list_interfaces isaac_ros_nitros_bridge_interfaces isaac_ros_test_cmake isaac_ros_visual_slam_interfaces isaac_ros_visual_slam nvblox_msgs nvblox_nav2 nvblox_ros_common nvblox_image_padding nvblox_rviz_plugin nvblox_ros nvblox_test_data semantic_label_conversion nvblox_examples_bringup isaac_ros_nvblox; do
  if [ -f "/home/jetson/isaac_ws_ros/install/$pkg/share/$pkg/local_setup.bash" ]; then
    echo "EXISTS: $pkg"
  else
    echo "MISSING: $pkg"
  fi
done
# isaac_ws_ros 워크스페이스 내의 주요 패키지들에 대해 local_setup.bash 파일이 존재하는지 루프를 통해 확인
```
### 3.3.2 분석 결과
<details>
<summary>루프 결과 출력 (Click)</summary>

```bash
MISSING: isaac_common
MISSING: isaac_ros_apriltag_interfaces
MISSING: isaac_ros_bi3d_interfaces
MISSING: isaac_ros_common
MISSING: isaac_ros_nova_interfaces
MISSING: isaac_ros_pointcloud_interfaces
MISSING: isaac_ros_r2b_galileo
MISSING: isaac_ros_tensor_list_interfaces
MISSING: isaac_ros_nitros_bridge_interfaces
MISSING: isaac_ros_test_cmake
MISSING: isaac_ros_visual_slam_interfaces
MISSING: isaac_ros_visual_slam
MISSING: nvblox_msgs
MISSING: nvblox_nav2
MISSING: nvblox_ros_common
MISSING: nvblox_image_padding
MISSING: nvblox_rviz_plugin
MISSING: nvblox_ros
MISSING: nvblox_test_data
MISSING: semantic_label_conversion
MISSING: nvblox_examples_bringup
MISSING: isaac_ros_nvblox

```
</details>

- 결과 : 모든 패키지에서 local_setup.bash 파일이 "MISSING"으로 표시됨
- 의미 : 파일이 존재하는 것은 확인되었지만, 현재 사용자(또는 터미널 세션)가 해당 파일에 접근할 수 없는 상태임. 이는 심볼릭 링크가 깨졌거나, 파일 권한이 잘못 설정되어 있거나, Docker 컨테이너 내부에서 빌드된 파일이 호스트에서는 접근할 수 없는 상태일 가능성이 높음.
---

### 3.4 심볼릭 링크 문제 확인 - 파일은 존재하는데 왜 실행이 안되는지 분석

### 3.4.1 전체 구조 확인
```bash
ls /home/jetson/isaac_ws_ros/install/ | head -30
# isaac_ws_ros 워크스페이스의 install 폴더 내에 어떤 파일과 폴더들이 있는지 확인
```

### 3.4.2 분석 결과
```bash
cmake  environment  hook  local_setup.bash  local_setup.dsv  local_setup.sh  local_setup.zsh  package.bash  package.dsv  package.ps1  package.sh  package.xml  package.zsh  version_info.yaml
# 결과 : install 폴더 내에는 local_setup.bash 파일이 존재하지만, share 폴더나 각 패키지별 폴더가 보이지 않음
# 의미 : 심볼릭 링크가 깨졌거나, 빌드 과정에서 install
```

### 3.4.3 
```bash
test -f /home/jetson/isaac_ws_ros/install/isaac_common/share/isaac_common/local_setup.bash && echo "EXISTS" || echo "MISSING"
```
### 3.4.4 분석 결과
```bash
MISSING
```

### 3.4.5
```bash
file /home/jetson/isaac_ws_ros/install/isaac_common/share/isaac_common/local_setup.bash
# 해당 파일이 심볼릭 링크인지, 일반 파일인지, 혹은 다른 유형의 파일인지 확인
```
### 3.4.6 분석 결과
```bash
/home/jetson/isaac_ws_ros/install/isaac_common/share/isaac_common/local_setup.bash: broken symbolic link to /workspaces/isaac_ros-dev/build/isaac_common/ament_cmake_environment_hooks/local_setup.bash
# 결과 : 해당 파일은 심볼릭 링크이며, 원본 경로는 /workspaces/isaac_ros-dev/build/isaac_common/ament_cmake_environment_hooks/local_setup.bash임. 그러나 "broken symbolic link"라는 메시지는 원본 파일이 존재하지 않거나, 심볼릭 링크가 올바르게 연결되어 있지 않다는 것을 의미함.
# 의미 : Docker 컨테이너 내부에서 빌드된 파일이 호스트에서는 접근할 수 없는 상태임. 즉, 심볼릭 링크가 Docker 내부의 경로를 가리키고 있기 때문에, 호스트에서는 해당 파일을 찾을 수 없어서 "not found" 메시지가 나오는 것임.
```

- 원인을 정확히 찾dma. --symlink-install로 Docker 컨테이너 안에서 빌드했기 때문에 심볼릭 링크가 컨테이너 내부 경로(/workspaces/isaac_ros-dev/build/...)를 가리키고 있어서, 호스트에서는 깨진 링크가 됨.
- 
### 3.4.7
```bash
find /home/jetson/isaac_ws_ros/install -name "local_setup.bash" -type l | head -5 | xargs -I{} readlink {}
# install 폴더 내에서 local_setup.bash라는 이름의 심볼릭 링크를 찾아서, 그 링크가 가리키는 원본 경로를 출력
```

### 3.4.8 분석 결과
```bash
/workspaces/isaac_ros-dev/build/isaac_ros_pointcloud_interfaces/ament_cmake_environment_hooks/local_setup.bash
/workspaces/isaac_ros-dev/build/nvblox_image_padding/ament_cmake_environment_hooks/local_setup.bash
/workspaces/isaac_ros-dev/build/isaac_common/ament_cmake_environment_hooks/local_setup.bash
/workspaces/isaac_ros-dev/build/nvblox_ros/ament_cmake_environment_hooks/local_setup.bash
/workspaces/isaac_ros-dev/build/isaac_ros_visual_slam/ament_cmake_environment_hooks/local_setup.bash
# 결과 : 모든 심볼릭 링크가 /workspaces/isaac_ros-dev/build/... 경로를 가리키고 있음
# 의미 : 이 경로는 Docker 컨테이너 내부의 경로일 가능성이 높음. 따라서 호스트에서는 해당 파일을 찾을 수 없어서 "not found" 메시지가 나오는 것임. 즉, 심볼릭 링크가 Docker 내부의 경로를 가리키고 있기 때문에, 호스트에서는 해당 파일을 찾을 수 없어서 "not found" 메시지가 나오는 것임.
```

### 3.4.9
```bash
cat /home/jetson/jupiter_ws_ros2/install/setup.bash
# setup.bash 파일의 내용을 출력하여, 심볼릭 링크가 어떻게 기록되어 있는지 확인
```

### 3.4.10 분석 결과
<details>
<summary>setup.bash 내용 출력 (Click)</summary>

```bash
# generated from colcon_bash/shell/template/prefix_chain.bash.em

# This script extends the environment with the environment of other prefix
# paths which were sourced when this file was generated as well as all packages
# contained in this prefix path.

# function to source another script with conditional trace output
# first argument: the path of the script
_colcon_prefix_chain_bash_source_script() {
  if [ -f "$1" ]; then
    if [ -n "$COLCON_TRACE" ]; then
      echo "# . \"$1\""
    fi
    . "$1"
  else
    echo "not found: \"$1\"" 1>&2
  fi
}

# source chained prefixes
# setting COLCON_CURRENT_PREFIX avoids determining the prefix in the sourced script
COLCON_CURRENT_PREFIX="/opt/ros/humble"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"
# setting COLCON_CURRENT_PREFIX avoids determining the prefix in the sourced script
COLCON_CURRENT_PREFIX="/home/jetson/yahboomcar_ros2_ws/software/library_ws/install"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"
# setting COLCON_CURRENT_PREFIX avoids determining the prefix in the sourced script
COLCON_CURRENT_PREFIX="/home/jetson/yahboomcar_ros2_ws/yahboomcar_ws/install"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"
# setting COLCON_CURRENT_PREFIX avoids determining the prefix in the sourced script
COLCON_CURRENT_PREFIX="/home/jetson/isaac_ws_ros/install"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"

# source this prefix
# setting COLCON_CURRENT_PREFIX avoids determining the prefix in the sourced script
COLCON_CURRENT_PREFIX="$(builtin cd "`dirname "${BASH_SOURCE[0]}"`" > /dev/null && pwd)"
_colcon_prefix_chain_bash_source_script "$COLCON_CURRENT_PREFIX/local_setup.bash"

unset COLCON_CURRENT_PREFIX
unset _colcon_prefix_chain_bash_source_script
```
</details>

- 결과 : setup.bash 파일 내에서 심볼릭 링크가 어떻게 기록되어 있는지 확인할 수 있음. 특히, COLCON_CURRENT_PREFIX="/home/jetson/isaac_ws_ros/install" 부분이 중요함. 이 부분이 심볼릭 링크 문제의 원인임을 명확히 보여줌.
- 의미 : setup.bash 파일이 Docker 컨테이너 내부에서 빌드된 패키지들의 local_setup.bash 파일을 참조하려고 시도하지만, 호스트에서는 해당 파일들이 존재하지 않아서 "not found" 메시지가 나오는 것임. 즉, 심볼릭 링크가 Docker 내부의 경로를 가리키고 있기 때문에, 호스트에서는 해당 파일을 찾을 수 없어서 "not found" 메시지가 나오는 것임

### 3.5.1 결론 및 해결 방안
```bash
env -i HOME=$HOME USER=$USER TERM=$TERM PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin LANG=${LANG:-en_US.UTF-8} bash --norc --noprofile -c '
> source /opt/ros/humble/setup.bash
> source /home/jetson/yahboomcar_ros2_ws/software/library_ws/install/setup.bash
> source /home/jetson/yahboomcar_ros2_ws/yahboomcar_ws/install/setup.bash
> echo "=== Current ROS env ==="
> echo "ROS_DISTRO=$ROS_DISTRO"
> echo "AMENT_PREFIX_PATH=$AMENT_PREFIX_PATH" | tr ":" "\n"
> echo "=== No isaac_ws_ros in prefix ==="
> cd /home/jetson/jupiter_ws_ros2
> colcon build --symlink-install 2>&1
> '
# Docker 컨테이너 내부에서 빌드된 패키지들이 호스트에서는 접근할 수 없는 상태임을 확인하기 위한 명령
# env -i는 깨끗한 환경에서 명령을 실행하도록 하는 옵션
# HOME, USER, TERM, PATH, LANG 등의 환경 변수를 명시적으로 설정하여, Docker 내부에서 빌드된 패키지들이 호스트에서는 접근할 수 없는 상태임을 확인
# bash --norc --noprofile -c '...'는 새로운 Bash 세션을 시작하여, 명령을 실행하도록 하는 부분
# source 명령어로 ROS2 Humble과 야붐카 관련 워크스페이스를 로드한 후, 현재 ROS 환경 변수를 출력하여 어떤 패키지들이 포함되어 있는지 확인
# colcon build --symlink-install 명령을 실행하여 빌드 과정에서 어떤 오류가 발생하는지 확인
```

### 3.5.2 제거 확인
```bash
grep "isaac_ws_ros" /home/jetson/jupiter_ws_ros2/install/setup.bash
```
### 3.5.3 분석 결과
- 출력없음
- 의미 : setup.bash 파일에서 isaac_ws_ros에 대한 참조가 제거되었음을 확인. 이제 터미널을 새로 열 때마다 "not found" 메시지가 사라질 것으로 예상됨.

```bash
## 7. 수정된 파일 목록

| 파일 | 변경 유형 | 변경 내용 |
|------|-----------|-----------|
| `nav2_vslam_fused.launch.py` | 수정 | sllidar, rf2o, rf2o_cov_adapter, slam_toolbox 노드 추가, 아키텍처 다이어그램 업데이트, lidar_port arg 추가 |
| `ekf_vslam_fusion.yaml` | 수정 | odom2 섹션 추가, 미지원 covariance 파라미터 제거, 헤더 주석 업데이트 |
| `vslam_covariance_adapter.py` | 변경 없음 | rf2o_covariance_adapter로 재사용 (다른 파라미터) |

---

## 8. 파일 경로

| 파일 | 경로 |
|------|------|
| 센서융합 launch | `~/jupiter_ws_ros2/src/jupiter_nav_VO/launch/nav2_vslam_fused.launch.py` |
| EKF 설정 | `~/jupiter_ws_ros2/src/jupiter_nav_VO/config/ekf_vslam_fusion.yaml` |
| 공분산 어댑터 | `~/jupiter_ws_ros2/src/jupiter_nav_VO/jupiter_nav_VO/vslam_covariance_adapter.py` |
| SLAM Toolbox 설정 | `~/jupiter_ws_ros2/src/jupiter_nav/config/slam_params.yaml` |
| TIL 문서 | `~/jupiter_ws_ros2/TIL_2026_02_24_RF2O_SLAM_Integration.md` |
